version: "2"

linters:
  default: standard
  enable:
    - copyloopvar
    - errorlint
    - gocritic
    - misspell
    - noctx
    - prealloc
    - revive
    - unparam
    - usestdlibvars

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-same-issues: 0
